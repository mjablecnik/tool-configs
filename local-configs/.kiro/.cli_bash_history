#V2
/mcp
Spust si tento projekt na localhostu v dockeru a s pomocí playwright otestuj, zda tam funguje vše tak jak má
Pokracuj
/mcp
Ověř zda tento projekt již běží v dockeru a s pomocí playwright otestuj, zda tam funguje vše tak jak má
Chci zde ještě vytvořit synchronizační script který bude používat rsync a vždy tu aktualni složku kde tento script spustím se mi přes rsync synchronizuje s jinym pocitacem, pouzij prikaz: rsync -av --exclude-from=/home/martin/.rsyncignore ../directus martin@sandbox:/home/martin/Projects   a updav ho tak aby tam posílal vždy aktuální adresář kde ho zrovna spouštím. Ten script chci spouštět ve verzich přenezení odtud tam a odtamtud sem. Přidej také help a verbose parametry, help bude i obsahovat příklad použití
do instal scriptu pridej tento script at se instaluje do stejne slozky
